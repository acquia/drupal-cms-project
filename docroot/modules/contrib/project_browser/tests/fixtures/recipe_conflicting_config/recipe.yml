name: 'Recipe with conflicting config'
type: 'Testing'
description: 'Ships a strict config that diverges from the active site configuration, so the recipe can only be instantiated before it is applied. Used to verify that an applied recipe which can no longer be loaded does not blank the project browser listing.'
