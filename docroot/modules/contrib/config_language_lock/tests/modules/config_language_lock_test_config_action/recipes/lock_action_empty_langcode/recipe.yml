name: 'Config language lock action empty langcode test'
install:
  - config_language_lock_test_types
config:
  actions:
    config_language_lock_test_types.always_valid.lock_action_empty:
      createIfNotExists:
        langcode: ''
        label: 'Lock action empty langcode'
        id: lock_action_empty
